ローカルで変更があったファイルを確認する
git status

現在使用しているブランチを確認する
git branch

リモートのブランチを確認する
git branch -a

リモートから最新のリポジトリ情報を取得し、更新する。
※情報を更新するだけで、実際にローカルブランチの内容が更新されるわけではありません
git fetch

ブランチをマージする。ローカルブランチが最新のものに更新される。
git merge　ブランチ名　例：origin/master
